#include<iostream>
using namespace std;
int main(){
    int base, height;
    cout<< " enter base and height"<< endl;
    cin >> base >> height;
    int formula = ( base * height ) / 2;
    cout << " so the answere is " << formula << endl;
}